spring:
  datasource:
    url: jdbc:mysql://localhost:3306/document?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    #这里如果为true,则表示在每次查询数据库的时候，自动打印查询的语句
    show-sql: false
    properties:
      hibernate:
        format_sql: true
server:
  port: 8182